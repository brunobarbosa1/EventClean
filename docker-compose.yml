version: '3.8'
services:
  postgres:
    image: postgres:latest
    container_name: eventclean-postgres
    environment:
      POSTGRES_DB: eventos
      POSTGRES_USERNAME: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5431:5432"
    restart: always